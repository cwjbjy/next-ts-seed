import { Button } from 'antd';
import Link from 'next/link';

const Home = () => (
  <div className="App">
    <Link href={''}></Link>
    <Button type="primary">Button</Button>
  </div>
);

export default Home;
